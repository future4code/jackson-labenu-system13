export type Main = {
    id: number,
    name: string,
    email: string,
    birth_date: Date,
    mission_id: number
}

export enum ESPECIALT {
    REACT = "REACT",
    REDUX = "REDUX",
    CSS = "CSS",
    TESTES = "TESTES",
    TYPESCRIPT = "TYPESCRIPT",
    DOM = "DOM",
    BACKEND = "BACKEND"
}

